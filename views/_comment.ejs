<li>
                    
    <p>
       
        <%= comment.content %>
        <br>
        <small>
            <%= comment.user.name %>
            <!-- <%= comment.user.email %> -->
        </small>

        <% if (locals.user && locals.user.id == comment.user.id){ %>
            <button>
                <a href="/comments/destroy/<%= comment.id %>"> Delete Comment </a>
            </button>
        <% } %>
    
    </p>  

</li>